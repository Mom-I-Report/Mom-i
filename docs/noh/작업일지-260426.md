# 2026-04-26 작업 일지

| 항목 | 내용 |
|------|------|
| **브랜치** | feat/noh |
| **작업자** | 노호종 |

---

## 1. 프롬프트 6파일 분리 (gemini_client.py / prompts/)

기존 `system_prompt.md` 단일 파일 → 역할별 6개 파일로 분리

| 파일 | 역할 |
|------|------|
| `system.md` | AI 정체성, HARD RULES 7개, STYLE |
| `knowledge.md` | 판단 기준 전체 (AAP 수면·호흡·체온·수면법·원더윅스) |
| `age_policy.md` | 월령별 수면법 허용·금지 (신규) |
| `reasoning.md` | 추론 규칙 및 제약 (신규) |
| `input_template.md` | user prompt 템플릿 |
| `output_format.md` | JSON 출력 형식 + 최종 규칙 |

- 기존 `prompt/` 폴더(dead code) 역할 완전히 대체
- 서버 시작 시 6파일을 조합해 `system_instruction` 구성

**코드 변경 (`gemini_client.py`)**:
```python
_SYSTEM_FILES = ["system.md", "knowledge.md", "age_policy.md", "reasoning.md"]
_SYSTEM_PROMPT = "\n\n---\n\n".join(
    (_PROMPTS_DIR / f).read_text(encoding="utf-8") for f in _SYSTEM_FILES
) + "\n\n---\n\n" + (_PROMPTS_DIR / "output_format.md").read_text(encoding="utf-8")
```

---

## 2. 도메인 지식 코드 매핑 검증

`knowledge.md`의 판단 기준이 코드에 올바르게 반영되었는지 검증

| 항목 | 판정 주체 |
|------|----------|
| AAP 수면 시간 기준 | ✅ 코드 직접 계산 (`_build_pattern_summary`) |
| AAP 호흡수 기준 | ✅ 코드 직접 계산 (`_BREATH_RANGES`) |
| 체온 기준 | ✅ 코드 직접 계산 (`_get_body_temp_status`) |
| 뒤척임 해석 | ✅ 코드 직접 계산 (`_build_pattern_summary`) |
| 실내 환경 기준 | ⚙️ Gemini 위임 (의도적 — 복합 요소 판단) |
| 수면 교육법 선택 | ⚙️ Gemini 위임 + `age_policy.md` 제약 |
| 원더윅스 판정 | ⚙️ Gemini 위임 |

---

## 3. knowledge.md 수면 교육법 확장

기존 5개 → 7개로 확장

| 추가 교육법 | 특징 |
|-----------|------|
| 안눈법 (No Tears) | 울음 최소화, 즉각 반응, 민감한 아기·부모 적합 |
| 픽업앤다운 | 울면 안고 진정 후 내려놓기 반복, 중간 단계 전환기 |

---

## 4. SleepGuide kick_action 필드 추가

```json
"sleep_guide": {
  "method_name": "...",
  "title": "...",
  "reason": "...",
  "steps": ["..."],
  "kick_action": "오늘 밤 바로 ___ 해보세요"
}
```

변경 파일: `schemas.py`, `output_format.md`, `gemini_client.py` (`_SLEEP_GUIDE_FIELDS`)

---

## 5. report_service.py — pattern_summary 신규

`_build_pattern_summary()` 함수 추가: 월령 대비 수면량·뒤척임·호흡/체온 이상 여부를 한 줄 텍스트로 변환해 Gemini user prompt에 주입

```
출력 예시: "수면 정상 범위 / 뒤척임 과다 / 미열 주의"
```

---

## 6. 버그 수정

| 버그 | 파일 | 수정 내용 |
|------|------|---------|
| `sleep_guide: null` → TypeError | `report_service.py` | `ai_result.get("sleep_guide")` falsy 안전 처리 |
| 체온 판정 기준 오류 | `report_service.py` | 절대 체온(37.4°C) → EMTAKE 델타값(0.9°C) 기준으로 수정 |
