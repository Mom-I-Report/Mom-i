# 2026-04-27 작업 일지

| 항목 | 내용 |
|------|------|
| **브랜치** | feat/noh |
| **작업자** | 노호종 |

---

## 1. 프론트엔드 렌더링 통일 (demo.html / admin.html)

demo.html과 admin.html이 동일 `report_json`을 서로 다르게 렌더링하는 문제 해결

**공유 모듈 추출 (`frontend/public/`)**:
- `report-renderer.js` — `renderReportBody(container, rj, opts)` 공용 렌더러
- `report.css` — 공유 스타일

렌더링 섹션 순서 통일:
1. 주간 요약 (레이더 차트 + stat-stack 5행)
2. 일별 수면 (막대+꺾은선 차트)
3. AI 솔루션 (sleep_guide + kick_action)
4. AI 코멘트 (ai_comment)
5. 월령 키워드 (age_kick)

**페이지별 차이점**:

| 항목 | demo.html | admin.html |
|------|-----------|-----------|
| showTrend | false | true |
| 탭 전환 | 없음 | 최근 3주 탭 |

---

## 2. 관리자 기기 API 추가 (admin_api.py)

```
GET /api/v1/admin/devices                    → 전체 기기 목록 (ser_no, 구독일, 리포트 수)
GET /api/v1/admin/devices/{ser_no}/reports  → 기기별 최근 3주치 리포트
```

---

## 3. Gemini SDK 전환 (google-generativeai → google-genai)

`requirements.txt` 및 `gemini_client.py` 임포트 방식 변경 완료

---

## 4. 버그 수정

| 버그 | 원인 | 수정 |
|------|------|------|
| `0h` / `0분` 트렌드 뱃지 오표시 | `val === 0` 미처리 | `|| val === 0` 조건 추가 |
| kick_action 미표시 | 렌더링 블록 누락 | 조건부 렌더링 추가 |

---

## 5. 랜딩 페이지 신규 (index.html)

`localhost:5173` 루트 접속 시 빈 화면 → Demo / Admin 바로가기 카드 2개로 교체
