# 개발 환경 전용 오버라이드 (docker compose up 시 자동 적용)
# 운영 배포 시: docker compose -f docker-compose.yml up -d
services:
  db:
    ports:
      - "3306:3306"

  app:
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
